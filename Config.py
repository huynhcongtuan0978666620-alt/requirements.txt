# config.py - HỆ THỐNG MÀU SẮC PHONG CÁCH MB BANK (CHI TIẾT)

THEME_COLORS = {
    # 1. NHÓM MÀU NỀN (BACKGROUNDS) - Tông xám trắng sang trọng của MB
    "bg_app": "#f4f6f8",          # Nền tổng thể toàn trang (tông xám nhạt MB)
    "bg_card": "#ffffff",         # Nền các thẻ (trắng sạch)
    "bg_box_chung": "#f4f7f6",    # Nền cho các ô chứa nội dung phụ
    "bg_vip_box": "#e3f2fd",      # Nền riêng cho khu vực VIP (Xanh nhạt MB)
    "bg_shake_box": "#fff3e0",    # Nền cho hiệu ứng lắc/thông báo
    "bg_badge_hang": "#efebe9",   # Nền các nhãn hạng thành viên
    "bg_marquee": "#0068ff",      # Nền chữ chạy dùng xanh dương MB để nổi bật

    # 2. NHÓM MÀU ĐIỂM NHẤN (ACCENTS) - Xanh thương hiệu MB
    "primary": "#0068ff",         # Màu xanh chủ đạo MB
    "accent_vip": "#ffb300",      # Màu vàng sang trọng cho VIP
    "accent_danger": "#e53935",   # Đỏ cảnh báo (Sắc nét)
    "accent_zalo": "#0068ff",     # Xanh Zalo (Đồng bộ với xanh MB)
    "accent_chiet_khau": "#e65100", # Cam cho con số chiết khấu

    # 3. NHÓM MÀU CHỮ (TYPOGRAPHY) - Chữ xanh tím chuyên nghiệp
    "text_main": "#1a237e",       # Chữ chính (xanh tím đậm - rất rõ nét)
    "text_secondary": "#546e7a",  # Chữ phụ (xám xanh)
    "text_muted": "#90a4ae",      # Chữ mờ (vô hiệu hóa)
    "text_title": "#1a237e",      # Tiêu đề đậm (sang trọng, dễ đọc)
    "text_badge": "#ffffff",      # Chữ trên các nhãn (trắng)

    # 4. MÀU ĐƯỜNG VIỀN & ĐỔ BÓNG (BORDERS & SHADOWS)
    "border_light": "#e0e0e0",    # Đường kẻ mảnh tinh tế của MB
    "border_input": "#cfd8dc",    # Viền khung nhập liệu
    "border_badge": "#b2dfdb",    # Viền nhãn thành viên
    "border_vip": "#4fc3f7",      # Viền trang trí cho VIP
    
    # Đổ bóng (Shadows) - Tạo độ nổi khối chuẩn App ngân hàng
    "shadow_light": "0 2px 8px rgba(0, 0, 0, 0.05)",   # Đổ bóng nhẹ
    "shadow_heavy": "0 8px 24px rgba(0, 0, 0, 0.12)",   # Đổ bóng nổi khối
    "shadow_toast": "rgba(0, 0, 0, 0.6)",               # Đổ bóng cho thông báo nổi
}
